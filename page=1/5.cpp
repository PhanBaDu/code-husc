#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;

    // Số lượng số chia hết cho 3 từ 1 đến n-1
    long long dem = (n) / 3;

    cout << dem;
    return 0;
}
